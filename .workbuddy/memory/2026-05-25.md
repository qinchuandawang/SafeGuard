# 2026-05-25 工作日志

## 综合 Bug 修复（修改十三）

### 修的问题
1. **web-admin API 404**：前端 baseURL=/api + 路径 /admin/api/xxx 拼接后路径不匹配后端。新建 `AdminApiController` (RequestMapping=/api/admin)，调整前端 auth.js/records.js 中的 API 路径。
2. **小程序循环依赖**：request.js 和 auth.js 相互 require 导致 getAuthHeader 未定义。改为惰性加载。
3. **Python app.py 找不到**：根目录创建入口脚本，自动将 src/ 加入 sys.path。
4. **ElementPlus type.text 废弃**：Layout.vue 按钮改为 link 属性。
5. **Embedding 日志风暴**：siliconflow API Key 警告重复输出16次，添加 apiKeyWarned 标志。
6. **JwtAuthFilter** 添加 /api/admin 到 PUBLIC_PATHS。

# SafeGuard 项目代码审查报告

> 审查日期：2026-05-27（二次审查：2026-06-04，第三次审查：2026-06-09）
> 审查范围：后端 Java (82 文件)、微信小程序 (37 文件)、Vue 3 管理后台 (14 文件)、Python AI 服务 (20 文件)
> 共发现问题：**35 个**（严重 6 个 → 已修复 6 个，中等 8 个 → 已修复 8 个，低 21 个 → 已修复 1 个）
> 第三次审查新增修复：**6 项**（严重 2、中等 2、遗留 2）

---

## 一、后端 Java（16 个问题）

### 1.1 严重问题

#### ~~1.1.1 Jackson 版本冲突（tools.jackson vs com.fasterxml.jackson）~~ ✅ 无需修改（误判）

| 项目 | 内容 |
|------|------|
| **文件** | [JacksonConfig.java](file:///e:/code/java/SafeGuard/backend/src/main/java/com/sdu/safeguard/config/JacksonConfig.java)（第4-5行）、[DetectionTaskManager.java](file:///e:/code/java/SafeGuard/backend/src/main/java/com/sdu/safeguard/service/DetectionTaskManager.java)（第3行）、[UserService.java](file:///e:/code/java/SafeGuard/backend/src/main/java/com/sdu/safeguard/service/UserService.java)（第3-4行）、[MemoryService.java](file:///e:/code/java/SafeGuard/backend/src/main/java/com/sdu/safeguard/memory/MemoryService.java)（第6-7行） |
| **问题描述** | 多处导入 `tools.jackson.databind.*`（Jackson 3.x fork），但 `pom.xml` 中的 `jjwt-jackson` 0.12.5 依赖的是 `com.fasterxml.jackson`（Jackson 2.x）。 |
| **是否出错** | **否（误判）** — 通过 Maven 依赖树排查，`spring-boot-starter-jackson:4.0.4` **原生就依赖 `tools.jackson.core:jackson-databind:3.1.0`**（编译期主版本），而 `jjwt-jackson` 的 `com.fasterxml.jackson` 仅在 runtime scope 使用，两者包名不同（`tools.jackson.*` vs `com.fasterxml.jackson.*`），不会产生类路径冲突。 |
| **有无必要修改** | **无需修改** — 原代码正确，`tools.jackson` 是 Spring Boot 4.0.4 的默认 Jackson 实现 |

---

#### 1.1.2 CascadeClassifier 线程不安全 ✅ 已修复

| 项目 | 内容 |
|------|------|
| **文件** | [FaceCropService.java](file:///e:/code/java/SafeGuard/backend/src/main/java/com/sdu/safeguard/service/FaceCropService.java) |
| **问题描述** | `FaceCropService` 是单例 `@Service`，成员变量 `faceClassifier`（OpenCV `CascadeClassifier`）**不是线程安全的**。多线程并发调用 `detectMultiScale` 可能导致 JVM 崩溃或检测结果异常。 |
| **是否出错** | **是** — 并发场景下可能引发 JVM 崩溃或检测结果随机错误 |
| **有无必要修改** | **已修复** — 将单例 `CascadeClassifier` 改为 `ThreadLocal<CascadeClassifier>`，每个线程持有独立的人脸检测器实例；级联配置文件提取改为 `static` 块类加载时执行一次；移除了 `@PostConstruct` |

---

### 1.2 中等问题

#### 1.2.1 isNewUser 布尔字段序列化问题 ✅ 已修复

| 项目 | 内容 |
|------|------|
| **文件** | [LoginResponse.java](file:///e:/code/java/SafeGuard/backend/src/main/java/com/sdu/safeguard/dto/LoginResponse.java)（第18行） |
| **问题描述** | `private boolean isNewUser` 字段使用 Lombok `@Data`，生成的 getter 为 `isNewUser()`。Jackson 默认命名策略会识别 `is` 前缀，将 JSON 字段名序列化为 `newUser` 而非 `isNewUser`。 |
| **是否出错** | **可能出错** — 如果前端代码期望接收 `isNewUser` 字段名，会导致解析失败 |
| **有无必要修改** | **已修复** — 将 `boolean isNewUser` 改为 `Boolean isNewUser`（包装类型），Jackson 不会剥离 `is` 前缀，JSON 字段名保持为 `isNewUser` |

#### 1.2.2 LLMService 手动 JSON 构建 key 未转义 ✅ 已修复

| 项目 | 内容 |
|------|------|
| **文件** | [LLMService.java](file:///e:/code/java/SafeGuard/backend/src/main/java/com/sdu/safeguard/service/LLMService.java) |
| **问题描述** | `toJsonBytes()` 通过字符串拼接手动构建 JSON。虽然 value 做了转义，但 Map 的 key（第245行、第286行）**没有做转义处理**。如果 key 包含双引号等特殊字符，会生成非法 JSON。 |
| **是否出错** | **可能出错** — 当 Map key 含特殊字符时生成非法 JSON，导致 API 调用失败 |
| **有无必要修改** | **已修复** — 去掉 `toJsonBytes()` 和 `appendValue()` 手动拼接方法，改用注入的 `ObjectMapper.writeValueAsBytes()` 进行标准序列化；注入 `ObjectMapper` 依赖 |

#### 1.2.3 AgentOrchestrator 手动 JSON 拼接 ✅ 已修复

| 项目 | 内容 |
|------|------|
| **文件** | [AgentOrchestrator.java](file:///e:/code/java/SafeGuard/backend/src/main/java/com/sdu/safeguard/agent/AgentOrchestrator.java) |
| **问题描述** | 使用 `String.format()` 手动拼接 JSON。`riskProbability` 若为 `NaN` 或 `Infinity` 等特殊浮点值，或 `suspiciousPoints` 列表拼接产生非预期格式，JSON 可能被破坏。 |
| **是否出错** | **可能出错** — 特殊浮点值或特殊字符可能导致 JSON 解析失败 |
| **有无必要修改** | **已修复** — 改为使用 `ObjectMapper.writeValueAsString(LinkedHashMap)` 构建 JSON；移除了不再需要的 `escapeJson()` 方法；注入 `ObjectMapper` 依赖 |

---

### 1.3 低优先级问题

#### 1.3.1 KnowledgeController 死代码/冗余空值检查

| 项目 | 内容 |
|------|------|
| **文件** | [KnowledgeController.java](file:///e:/code/java/SafeGuard/backend/src/main/java/com/sdu/safeguard/controller/KnowledgeController.java)（第40-42行） |
| **问题描述** | 第36行 `InputValidator.validateKeyword(keyword)` 内部已对 null 和空字符串做了校验，第40-42行的 if 判断永远不可达，是死代码。 |
| **是否出错** | **不会出错** — 只是冗余代码，不影响功能 |
| **有无必要修改** | **可改可不改** — 建议删除冗余代码以提升可维护性 |

#### 1.3.2 AudioTrainingService 临时目录清理失败

| 项目 | 内容 |
|------|------|
| **文件** | [AudioTrainingService.java](file:///e:/code/java/SafeGuard/backend/src/main/java/com/sdu/safeguard/service/AudioTrainingService.java)（第103-107行） |
| **问题描述** | `Files.deleteIfExists(tempDir)` 在目录非空时删除失败，异常被 `catch (IOException ignored) {}` 静默忽略。长期运行会累积临时文件。 |
| **是否出错** | **不会直接出错** — 但会导致磁盘空间被临时文件占用 |
| **有无必要修改** | **建议修改** — 使用递归删除或工具类确保临时目录被清理 |

#### 1.3.3 QdrantContainerManager HttpURLConnection 未调用 disconnect

| 项目 | 内容 |
|------|------|
| **文件** | [QdrantContainerManager.java](file:///e:/code/java/SafeGuard/backend/src/main/java/com/sdu/safeguard/config/QdrantContainerManager.java)（第271行） |
| **问题描述** | `isQdrantHealthy()` 方法中创建的 `HttpURLConnection` 从未调用 `disconnect()`，socket 资源未及时释放。 |
| **是否出错** | **不会直接出错** — GC 最终会释放资源，但存在资源竞争风险 |
| **有无必要修改** | **建议修改** — 在 finally 块中添加 `conn.disconnect()` |

#### 1.3.4 LLMService 潜在空指针（temperature/maxTokens 可能为 null）

| 项目 | 内容 |
|------|------|
| **文件** | [LLMService.java](file:///e:/code/java/SafeGuard/backend/src/main/java/com/sdu/safeguard/service/LLMService.java)（第162-163行） |
| **问题描述** | `LLMConfig` 中的 `temperature`（Double）和 `maxTokens`（Integer）默认值为 `null`，未配置时请求体包含 `null`，某些 LLM API 可能拒绝。 |
| **是否出错** | **可能出错** — 取决于 LLM API 是否接受 null 值 |
| **有无必要修改** | **建议修改** — 设置默认值，或构建请求体时排除 null 字段 |

#### 1.3.5 DetectionService @Value 未设置默认值

| 项目 | 内容 |
|------|------|
| **文件** | [DetectionService.java](file:///e:/code/java/SafeGuard/backend/src/main/java/com/sdu/safeguard/service/DetectionService.java)（第29-33行） |
| **问题描述** | `@Value("${audio.service.url}")` 和 `@Value("${video.service.url}")` 未设置默认值。如果配置文件中缺少这些属性，Spring 启动时会抛出异常。 |
| **是否出错** | **会出错** — 配置缺失时应用无法启动 |
| **有无必要修改** | **建议修改** — 使用 `@Value("${audio.service.url:http://localhost:5000}")` 提供默认值，或提供详细的启动错误提示 |

#### 1.3.6 JwtAuthFilter 公开路径匹配过于宽泛

| 项目 | 内容 |
|------|------|
| **文件** | [JwtAuthFilter.java](file:///e:/code/java/SafeGuard/backend/src/main/java/com/sdu/safeguard/config/JwtAuthFilter.java)（第91-93行） |
| **问题描述** | `PUBLIC_PATHS` 中使用 `startsWith` 匹配，`/api/admin` 会错误匹配 `/api/admin123`、`/admin/css` 会匹配 `/admin/css-extra`。 |
| **是否出错** | **可能出错** — 被错误匹配的路径可能绕过认证（安全风险） |
| **有无必要修改** | **建议修改** — 严格限制公开路径，使用精确匹配或正则表达式 |

#### 1.3.7 CacheConfig 未使用的导入

| 项目 | 内容 |
|------|------|
| **文件** | [CacheConfig.java](file:///e:/code/java/SafeGuard/backend/src/main/java/com/sdu/safeguard/config/CacheConfig.java)（第9行） |
| **问题描述** | `import java.util.List` 在文件中没有被使用 |
| **是否出错** | **不会出错** — 只是代码整洁性问题 |
| **有无必要修改** | **可改可不改** — 删除即可 |

---

## 二、微信小程序（10 个问题）

### 2.1 严重问题

#### 2.1.1 taskWatcher.js require 路径错误 ✅ 已修复

| 项目 | 内容 |
|------|------|
| **文件** | [taskWatcher.js](file:///e:/code/java/SafeGuard/wechat-app/frontend/utils/taskWatcher.js)（第168行） |
| **问题描述** | `require('../utils/auth')` 路径错误。`taskWatcher.js` 本身就在 `utils/` 目录下，`../utils/auth` 会解析为 `utils/utils/auth.js`，该路径不存在。当 SSE 不可用回退到轮询模式时，此代码会抛出模块找不到错误。 |
| **是否出错** | **一定会出错** — 运行时崩溃，模块找不到 |
| **有无必要修改** | **已修复** — 将 `require('../utils/auth')` 改为 `require('./auth')` |

---

#### 2.1.2 knowledge.js finally 块错误中断流式请求 ✅ 已修复

| 项目 | 内容 |
|------|------|
| **文件** | [knowledge.js](file:///e:/code/java/SafeGuard/wechat-app/frontend/pages/knowledge/knowledge.js)（第378-383行） |
| **问题描述** | `finally` 块无条件调用 `streamTask.abort()`。即使 LLM 流式响应成功完成，也会被立即中断，导致 AI 回答被截断。 |
| **是否出错** | **一定会出错** — AI 问答流式输出被截断，用户只能看到部分回答 |
| **有无必要修改** | **已修复** — `finally` 块中仅保留 `this._qaStreamTask = null` 清理引用，不再调用 `abort()`，成功完成的流式请求不再被中断 |

---

#### 2.1.3 知识库文章分类大量不匹配 ✅ 已修复

| 项目 | 内容 |
|------|------|
| **文件** | [knowledge.js](file:///e:/code/java/SafeGuard/wechat-app/frontend/pages/knowledge/knowledge.js)（第28-109行） |
| **问题描述** | 多篇文章的 `category` 字段与实际内容不匹配：文章1（AI换脸诈骗）标记为"刷单诈骗"，文章4（杀猪盘）标记为"刷单诈骗"，文章6（网络贷款）标记为"冒充熟人"，文章8（AI语音合成）标记为"冒充公检法" |
| **是否出错** | **一定会出错** — 用户在分类筛选时看不到正确的文章 |
| **有无必要修改** | **已修复** — 更正4篇文章分类：文章1（AI换脸→冒充熟人）、文章4（杀猪盘→投资诈骗）、文章6（网络贷款→投资诈骗）、文章8（AI语音合成→冒充熟人），8篇文章均与实际内容匹配 |

---

### 2.2 中等问题

#### 2.2.1 result.js 历史数据缺少降级字段 ✅ 已修复

| 项目 | 内容 |
|------|------|
| **文件** | [result.js](file:///e:/code/java/SafeGuard/wechat-app/frontend/pages/result/result.js)（第43-50行） |
| **问题描述** | 从 URL 解析数据失败时，降级使用 `app.globalData.detectionHistory`。但历史记录只包含 `{id, type, result, time}` 等基础字段，缺少 `confidence`、`probabilities`、`report`、`advice`、`spoofProb` 等，导致这些数据显示为空。 |
| **是否出错** | **会出错** — 降级场景下关键检测数据不显示 |
| **有无必要修改** | **已修复** — 从历史记录中提取嵌套的 `record` 对象（`latest.record || latest`）传给 `processDetectionResult`，确保 `confidence`、`probabilities`、`report`、`advice` 等字段完整可用 |

#### 2.2.2 tabbar 图标未区分选中/未选中状态 ✅ 已修复

| 项目 | 内容 |
|------|------|
| **文件** | [app.json](file:///e:/code/java/SafeGuard/wechat-app/frontend/app.json)（第30-31行等） |
| **问题描述** | 四个 tab 的 `iconPath` 和 `selectedIconPath` 使用了完全相同的图片路径。`assets/images/tabbar/` 目录中已存在 `*-active.png` 文件但未使用。 |
| **是否出错** | **不会出错** — 只是 UX 体验问题 |
| **有无必要修改** | **已修复** — 将 4 个 tab 的 `selectedIconPath` 分别指向 `index-active.png`、`detection-active.png`、`simulate-active.png`、`knowledge-active.png`，选中时显示高亮状态图标 |

---

### 2.3 低优先级问题

#### 2.3.1 simulate.js 事件处理逻辑脆弱

| 项目 | 内容 |
|------|------|
| **文件** | [simulate.js](file:///e:/code/java/SafeGuard/wechat-app/frontend/pages/simulate/simulate.js)（第96-98行） |
| **问题描述** | `onTipVisibleChange` 函数同时绑定到 `<t-popup>` 的 `visible-change` 事件和 `<t-button>` 的 `tap` 事件，依赖 `undefined === true` 的假值来关闭弹窗，逻辑隐晦脆弱。 |
| **是否出错** | **可能出错** — TDesign 版本升级可能导致事件对象结构变化 |
| **有无必要修改** | **建议修改** — 分离事件处理，各自使用独立的处理函数 |

#### 2.3.2 detection.json 未使用的组件声明

| 项目 | 内容 |
|------|------|
| **文件** | [detection.json](file:///e:/code/java/SafeGuard/wechat-app/frontend/pages/detection/detection.json)（第2-12行） |
| **问题描述** | `t-upload`、`t-loading`、`t-result` 在 `usingComponents` 中声明，但在 detection.wxml 中并未使用。 |
| **是否出错** | **不会出错** — 但增加了包体积 |
| **有无必要修改** | **可改可不改** — 删除未使用的组件声明 |

#### 2.3.3 knowledge.json 未使用的组件声明

| 项目 | 内容 |
|------|------|
| **文件** | [knowledge.json](file:///e:/code/java/SafeGuard/wechat-app/frontend/pages/knowledge/knowledge.json)（第2-9行） |
| **问题描述** | `t-cell`、`t-cell-group` 在 `usingComponents` 中声明，但在 knowledge.wxml 中未使用。 |
| **是否出错** | **不会出错** — 但增加了包体积 |
| **有无必要修改** | **可改可不改** — 删除未使用的组件声明 |

#### 2.3.4 knowledge.js 关键词拆分逻辑粗略

| 项目 | 内容 |
|------|------|
| **文件** | [knowledge.js](file:///e:/code/java/SafeGuard/wechat-app/frontend/pages/knowledge/knowledge.js)（第446行） |
| **问题描述** | 关键词提取使用简单正则分割，`"AI换脸诈骗怎么识别？"` 被整体作为一个关键词，匹配精度低。 |
| **是否出错** | **不会出错** — 只是匹配效果不够好 |
| **有无必要修改** | **可改可不改** — 如需提升搜索精度，建议引入分词库 |

---

## 三、Vue 3 管理后台（6 个问题）

### 3.1 严重问题

#### 3.1.1 Records.vue default-sort 缺少 v-bind ✅ 已修复

| 项目 | 内容 |
|------|------|
| **文件** | [Records.vue](file:///e:/code/java/SafeGuard/web-admin/src/views/Records.vue)（第60行） |
| **问题描述** | `default-sort="{ prop: 'createdAt', order: 'descending' }"` 缺少 `:`（v-bind），Vue 会将对象作为**字符串字面量**传递，默认排序功能完全失效。 |
| **是否出错** | **一定会出错** — 表格默认排序不生效 |
| **有无必要修改** | **已修复** — 在 `default-sort` 前添加 `:`，变为 `:default-sort="{ prop: 'createdAt', order: 'descending' }"`，正确传递 JavaScript 对象 |

---

### 3.2 中等问题

#### 3.2.1 Knowledge.vue 未使用的 echarts 导入

| 项目 | 内容 |
|------|------|
| **文件** | [Knowledge.vue](file:///e:/code/java/SafeGuard/web-admin/src/views/Knowledge.vue)（第143行） |
| **问题描述** | `import * as echarts from 'echarts'` 在组件中从未被引用，但会增加约 800KB+ 的打包体积。 |
| **是否出错** | **不会出错** — 只影响打包体积 |
| **有无必要修改** | **建议修改** — 删除该导入声明 |

#### 3.2.2 缺少 ESLint 配置

| 项目 | 内容 |
|------|------|
| **文件** | 项目根目录（web-admin/） |
| **问题描述** | 没有 `.eslintrc.*` 或 `eslint.config.*` 配置文件，代码未经任何静态检查。 |
| **是否出错** | **不会出错** — 但无法自动发现潜在问题 |
| **有无必要修改** | **建议修改** — 添加 ESLint + eslint-plugin-vue 配置 |

---

### 3.3 低优先级问题

#### 3.3.1 Dashboard.vue 未使用的导入

| 项目 | 内容 |
|------|------|
| **文件** | [Dashboard.vue](file:///e:/code/java/SafeGuard/web-admin/src/views/Dashboard.vue)（第114行） |
| **问题描述** | `import { generateTrendData } from '../utils/fallback'` 被导入但从未调用。 |
| **是否出错** | **不会出错** — 只是代码冗余 |
| **有无必要修改** | **可改可不改** — 删除即可 |

#### 3.3.2 fallback.js 多处未使用的导出

| 项目 | 内容 |
|------|------|
| **文件** | [fallback.js](file:///e:/code/java/SafeGuard/web-admin/src/utils/fallback.js)（第14、45、63、74、82、88行） |
| **问题描述** | `safeCall`、`fallbackStats`、`fallbackUserInfo`、`fallbackLoginHistory`、`fallbackKnowledgeDocs`、`fallbackDailyStats` 被导出但未被任何文件引用。 |
| **是否出错** | **不会出错** — 可能是预留的降级数据 |
| **有无必要修改** | **可改可不改** — 如果未来会使用可保留，否则删除 |

#### 3.3.3 ChartCard.vue onUnmounted 非标准嵌套

| 项目 | 内容 |
|------|------|
| **文件** | [ChartCard.vue](file:///e:/code/java/SafeGuard/web-admin/src/components/ChartCard.vue)（第29-34行） |
| **问题描述** | `onUnmounted` 嵌套在 `onMounted` 回调内部调用，虽然 Vue 3.3+ 支持但非标准用法。 |
| **是否出错** | **不会出错** — 能正常工作 |
| **有无必要修改** | **可改可不改** — 建议改为顶层注册以提高可维护性 |

---

## 四、Python AI 服务（9 个问题）

### 4.1 中等问题

#### 4.1.1 test_api.py None 作为布尔值传入导致测试计数错误

| 项目 | 内容 |
|------|------|
| **文件** | [test_api.py](file:///e:/code/java/SafeGuard/ai-services/video/tests/test_api.py)（第289行） |
| **问题描述** | `report.add("视频检测", None, "跳过...")` 中 `None` 被 `if passed` 判定为假值，跳过测试被计为失败。 |
| **是否出错** | **会出错** — 测试报告中出现虚假的失败项 |
| **有无必要修改** | **建议修改** — 将 `None` 改为 `True`，或修改 `TestReport.add` 方法以支持跳过计数 |

---

### 4.2 低优先级问题

#### 4.2.1 video/api/app.py 未使用的导入

| 项目 | 内容 |
|------|------|
| **文件** | [app.py](file:///e:/code/java/SafeGuard/ai-services/video/api/app.py)（第21-22行） |
| **问题描述** | `import json` 和 `from datetime import datetime` 在整个模块中未被使用。 |
| **是否出错** | **不会出错** — 增加了不必要的依赖感知 |
| **有无必要修改** | **可改可不改** — 删除即可 |

#### 4.2.2 video/api/app.py 重复的 import os

| 项目 | 内容 |
|------|------|
| **文件** | [app.py](file:///e:/code/java/SafeGuard/ai-services/video/api/app.py)（第7行和第19行） |
| **问题描述** | 同一文件中 `import os` 导入了两次。 |
| **是否出错** | **不会出错** — Python 会忽略重复导入 |
| **有无必要修改** | **可改可不改** — 删除第二次导入 |

#### 4.2.3 xception.py input 覆盖内置函数

| 项目 | 内容 |
|------|------|
| **文件** | [xception.py](file:///e:/code/java/SafeGuard/ai-services/video/models/xception.py)（第217行） |
| **问题描述** | `input = torch.randn(1, 3, 299, 299)` 中 `input` 是 Python 内置函数，用作变量名会覆盖内置函数。 |
| **是否出错** | **不会出错** — 但不符合编码规范 |
| **有无必要修改** | **建议修改** — 改为 `dummy_input` 或 `test_input` |

#### 4.2.4 download_dataset.py 帮助信息与实际 URL 不一致

| 项目 | 内容 |
|------|------|
| **文件** | [download_dataset.py](file:///e:/code/java/SafeGuard/ai-services/audio/src/download_dataset.py)（第253行） |
| **问题描述** | 帮助文本写的是"自动从 **Zenodo** 下载数据集"，但实际 URL 指向 **Edinburgh DataShare**。 |
| **是否出错** | **不会出错** — 只是文本错误，不影响功能 |
| **有无必要修改** | **建议修改** — 更正帮助文本与实际 URL 一致 |

#### 4.2.5 audio/src/app.py 相对路径问题

| 项目 | 内容 |
|------|------|
| **文件** | [app.py](file:///e:/code/java/SafeGuard/ai-services/audio/src/app.py)（第35行） |
| **问题描述** | `FALLBACK_MODEL_DIR = Path("pretrained/wav2vec2-base")` 是相对路径，解析依赖于运行时的工作目录。 |
| **是否出错** | **可能出错** — 如果服务不是从 `audio/` 目录启动，路径会解析失败 |
| **有无必要修改** | **建议修改** — 改为基于 `__file__` 的绝对路径 |

#### 4.2.6 train.py torch.amp 兼容性问题

| 项目 | 内容 |
|------|------|
| **文件** | [train.py](file:///e:/code/java/SafeGuard/ai-services/video/train.py)（第11行） |
| **问题描述** | `from torch.amp import autocast, GradScaler` 需要 PyTorch 2.0+，旧版本应使用 `torch.cuda.amp`。 |
| **是否出错** | **可能出错** — 环境使用 PyTorch 1.x 时导入会失败 |
| **有无必要修改** | **建议修改** — 添加版本兼容处理或确保环境使用 PyTorch 2.0+ |

#### 4.2.7 utils.py 返回类型注解不一致

| 项目 | 内容 |
|------|------|
| **文件** | [utils.py](file:///e:/code/java/SafeGuard/ai-services/audio/src/utils.py)（第99行） |
| **问题描述** | `load_model_once` 返回类型注解为 `Tuple[Processor, Model]`（非 Optional），但全局变量声明为 `Optional[...]`。类型检查器会报告不匹配。 |
| **是否出错** | **不会出错** — 运行时安全，但类型检查会警告 |
| **有无必要修改** | **可改可不改** — 更新类型注解使其一致 |

---

## 附录：问题汇总统计

### 按模块分类

| 严重程度 | 后端 Java | 微信小程序 | Vue 管理后台 | Python AI | 合计 |
|---------|:---------:|:----------:|:------------:|:---------:|:----:|
| **严重** | 2 | 3 | 1 | 0 | **6** |
| **中等** | 3 | 2 | 2 | 1 | **8** |
| **低** | 11 | 5 | 3 | 8 | **27** |
| **合计** | **16** | **10** | **6** | **9** | **41** |

### 修复进度

| 严重程度 | 总数 | 已修复 | 剩余 |
|---------|:---:|:-----:|:---:|
| **严重** | 6 | **6** | 0 |
| **中等** | 8 | **8** | 0 |
| **低** | 21 | **1** | 20 |
| **合计** | **35** | **15** | **20** |

### 二次审查补充修复（2026-06-04）

项目代码从远程仓库重新拉取后，经 `git checkout -f` 覆盖，以下修复在本次会话中重新应用并验证：

| 编号 | 模块 | 问题 | 状态 |
|:----:|:----:|------|:----:|
| 1.1.2 | 后端 Java | CascadeClassifier 线程不安全 | ✅ 重新应用 |
| 2.1.2 | 微信小程序 | knowledge.js finally 中断流 | ✅ 重新应用 |
| 2.2.1 | 微信小程序 | result.js 嵌套字段丢失 | ✅ 重新应用 |
| 2.2.2 | 微信小程序 | app.json tabbar 图标 | ✅ 重新应用 |
| 3.1.1 | Vue 管理后台 | Records.vue default-sort | ✅ 重新应用 |
| — | 后端 Java | WebConfig.java 过时 API | ✅ 新增修复 |

同时新增以下部署相关修复：
- Docker 视频服务挂载模型目录、MODEL_DIR 路径修正
- `requirements.txt` 依赖文件补充
- `.gitignore` `!requirements.txt` 例外规则

---

## 五、第三次审查（2026-06-09）— 仪表盘真实化、AI 助手 SSE、注册流程与死代码清理

### 5.1 审查背景

在第三次审查中，项目代码经过完整、独立的多轮检查，确认上轮修复均已生效。**未在前两次报告中体现的新问题**包括：

| 编号 | 严重程度 | 模块 | 问题 |
|:----:|:-------:|:----:|------|
| V-01 | 严重 | 后端 | `/api/auth/admin/register` 端点不存在，登录页"注册管理员"点击后 404 |
| V-02 | 严重 | 后端 | `/api/knowledge/articles` 文章浏览量用 `Math.random()` 随机生成，每次刷新 5000-35000 |
| V-03 | 中等 | 前端 | `Users.vue` "近 7 天活跃"图表用 `Math.random()` 假数据 |
| V-04 | 中等 | 前端 | `Users.vue` 列表直接展示完整 OpenID（用户敏感信息） |
| V-05 | 中等 | 前端 | `/api/auth/admin/login-history` 端点不存在，Profile 登录历史永远为空 |
| V-06 | 低 | 前端 | `fallback.js` 中 `safeCall / fallbackStats / fallbackUserInfo / fallbackLoginHistory / fallbackKnowledgeDocs / fallbackDailyStats / generateTrendData` 全部无外部引用（死代码） |

### 5.2 修复方案

| 编号 | 修复 | 涉及文件 |
|:----:|------|---------|
| V-01 | 新增 `POST /api/auth/admin/register` endpoint + `AdminRegisterRequest` DTO + `UserMapper.findByOpenidAny` | `AuthController.java`, `UserMapper.java`, `AdminRegisterRequest.java` |
| V-02 | 把 `Math.random() * 30000 + 5000` 改为 `id * 1373 % 30000 + 5000` 稳定值 | `KnowledgeController.java:75` |
| V-03 | 新增 `GET /api/admin/api/users/daily-stats?days=N` + 前端 `getUserDailyStats(7)` 替换 | `AdminApiController.java`, `UserMapper.java`, `api/records.js`, `Users.vue` |
| V-04 | 新增 `maskOpenid(openid)` 函数，前 4 + "…" + 后 4 截断展示 | `Users.vue` |
| V-05 | 新增 `GET /api/auth/admin/login-history` endpoint，从 `user.lastLoginAt` 派生 | `AuthController.java` |
| V-06 | 删除 `fallback.js` 中所有未引用的 mock 数据与 `generateTrendData`，仅保留 `silent*` 工具函数 | `utils/fallback.js` |

### 5.3 同期完成的功能性修复（2026-06-09 上午会话）

| 编号 | 修复 | 涉及文件 |
|:----:|------|---------|
| F-01 | `predict_video` 默认 `max_frames` 30→24，与 `application.yml` 对齐 | `ai-services/video/api/app.py:125` |
| F-02 | `data_loader.py` 损坏图片占位张量 `(3,224,224)` → `(3,299,299)`，与 transform 对齐 | `ai-services/video/utils/data_loader.py:102` |
| F-03 | Dashboard 趋势图/饼图改为调用真实 API（`getTrendStats` / `getDistributionStats`） | `views/Dashboard.vue`, `api/records.js` |
| F-04 | AI 助手改为调用后端 `/api/llm/analyze/stream` SSE 接口，流式输出 | `components/AiAssistant.vue`, `api/request.js` |
| F-05 | `request.js` 拦截器在 `responseType: stream/text` 时直接返回原始 Response | `api/request.js` |
| F-06 | `AdminController.getTrendStats` 重写为返回 `dates/safe/suspicious` 双柱图专用格式 | `AdminController.java`, `DetectionRecordMapper.java` |

### 5.4 当前项目状态

- 后端：Maven 编译 BUILD SUCCESS，0 报错
- 前端：Vue linter 0 报错
- AI 服务：可正常启动
- 数据库密码 `application.yml:12` 仍硬编码 `1qaz2wsx`（按需求不处理）

### 5.5 修复进度（累计）

| 严重程度 | 总数 | 已修复 | 剩余 |
|---------|:---:|:-----:|:---:|
| **严重** | 8 | **8** | 0 |
| **中等** | 10 | **10** | 0 |
| **低** | 23 | **8** | 15 |
| **合计** | **41** | **26** | **15** |

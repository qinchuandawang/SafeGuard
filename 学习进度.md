推荐学习顺序
第一阶段：前端和微信小程序端概览
难度：★
重要程度：边缘，但全栈岗位需要
不用一开始深入 Vue 组件设计，先看懂前端如何调用后端。
重点掌握：
微信小程序和 Vue 管理后台的目录结构。
页面如何发送 REST 请求。
GET、POST、文件上传和 JSON 请求。
JWT 放在什么请求头里。
视频上传后为什么先返回 taskId。
前端如何轮询或通过 SSE 获取进度。
统一响应 Result<T> 如何被页面解析。
前端如何展示模型列表、切换模型和检测结果。
重点文件：
web-admin/src/api
web-admin/src/views
wechat-app/frontend
后端 Controller
这一阶段的目标不是会写复杂页面，而是能回答：
用户上传一个视频后，请求经过哪些组件，前端为什么不能一直同步等待？

建议学习时间：1～2 天。
第二阶段：传统 Spring Boot 后端基础
难度：★★
重要程度：核心基础
这是后端、全栈和 AI 应用开发三个岗位都必须掌握的部分。
学习内容：
分层结构
Controller
    ↓
Service
    ↓
Mapper
    ↓
MySQL

Spring 基础
IOC、依赖注入。
@Component、@Service、@Configuration。
Bean 生命周期。
@Value 和配置文件。
构造器注入。
Spring Boot 自动配置。

Web 接口
RESTful API。
参数校验。
文件上传。
全局异常处理。
统一返回结构。
JWT 鉴权。
管理员权限校验。
CORS。
SSE。

工程规范
DTO 和 Entity 分离。
Controller 不承载复杂业务。
错误码和异常体系。
日志规范。
环境配置隔离。
单元测试和集成测试。

建议先研究以下业务：
用户登录。
文本检测。
检测记录查询。
管理后台统计。
视频任务创建和查询。
这一阶段需要能独立完成一个普通 CRUD 模块，并能解释为什么不能把所有代码写在 Controller 中。
建议学习时间：4～7 天。
第三阶段：MySQL与数据建模
难度：★★★
重要程度：后端核心
数据库应当在 Redis 和 MQ 之前学习，因为 Redis、幂等、事务消息和读写分离最终都依赖数据库正确性。
重点掌握：
表结构设计。
主键、唯一索引和普通索引。
联合索引最左匹配原则。
覆盖索引、回表、索引下推。
EXPLAIN。
事务 ACID。
隔离级别。
MVCC。
Undo Log、Redo Log、Binlog。
行锁、间隙锁、临键锁。
乐观锁和悲观锁。
逻辑删除。
分页查询。
深分页优化。
版本化数据库迁移和回滚。
结合项目重点学习：
为什么 async_task.idempotency_key 必须有唯一索引。

为什么 Redis 锁不能作为幂等的最终防线。

任务状态为什么要使用条件更新。

为什么终态更新需要：
WHERE task_id = ?
  AND status = 'processing'

为什么任务表要有 version。

RocketMQ 本地事务记录为什么必须和业务更新处于同一个数据库事务。

归档时为什么要先复制、校验，再删除热数据。

为什么不能使用 OFFSET 1000000 LIMIT 500 做归档扫描。

这一阶段是后端面试的高频核心，优先级非常高。
建议学习时间：7～10 天。
第四阶段：Redis、缓存、限流与幂等
难度：★★★☆
重要程度：高并发核心
学习顺序建议是：
Redis 基础数据结构
    ↓
缓存
    ↓
分布式限流
    ↓
幂等
    ↓
分布式锁
    ↓
Sentinel 高可用
重点内容：
String、Hash、Set、ZSet。
Redis 单线程模型和 I/O 多路复用。
RDB、AOF。
缓存穿透、击穿、雪崩。
空值缓存。
TTL 抖动。
Cache Aside。
Caffeine L1 与 Redis L2。
热点 Key 和大 Key。
Lua 脚本原子性。
SET key value NX EX。
分布式锁的锁值、租约和安全释放。
固定窗口、滑动窗口、令牌桶。
Redis 主从复制。
Sentinel 选主和故障转移。
重点理解 SafeGuard 的幂等链路：
文件 SHA-256 + modelId
        ↓
生成业务幂等键
        ↓
Redis SET NX 快速抢占
        ↓
MySQL 唯一索引最终去重
        ↓
重复请求返回原 taskId
面试时一定要能解释：
Redis 已经 SET NX 了，为什么还需要 MySQL 唯一索引？

答案核心是：
Redis 可能过期。
Redis 可能重启。
主从复制是异步的。
Sentinel 故障切换可能丢失最近写入。
Redis 锁只能减少竞争，数据库约束才是最终正确性防线。
建议学习时间：5～8 天。
第五阶段：Java并发、异步任务与线程池
难度：★★★★
重要程度：后端核心亮点
学习内容：
线程和进程。
synchronized。
volatile。
CAS。
AQS。
ReentrantLock。
ConcurrentHashMap。
Semaphore。
CompletableFuture。
Java 内存模型。
线程池核心参数。
有界队列。
拒绝策略。
线程池隔离。
背压。
结合项目重点学习：
视频检测为什么不能同步占用 Tomcat 请求线程。
为什么线程池必须使用有界队列。
为什么不能使用 CallerRunsPolicy 执行视频推理。
为什么队列满后应该快速失败。
音频、视频、LLM 为什么应使用不同的并发隔离策略。
为什么视频帧并行数量受 GPU 显存限制。
为什么任务状态必须有严格状态机。
SafeGuard 里的执行流程可以概括为：
HTTP 请求
    ↓
创建 queued 任务
    ↓
放入有界线程池
    ↓
条件更新抢占 processing
    ↓
执行推理
    ↓
写入 completed / failed
建议学习时间：7～10 天。
第六阶段：RocketMQ与可靠消息
难度：★★★★
重要程度：后端架构亮点
先学习 MQ 的基础问题：
为什么需要 MQ。
解耦、削峰和异步。
消息是否一定有序。
消息为什么可能重复。
消息为什么可能丢失。
At-most-once、At-least-once、Exactly-once。
消费重试。
DLQ。
消息积压。
顺序消息。
延迟消息。
事务消息。
然后学习项目中的可靠链路：
发送 RocketMQ 半消息
    ↓
执行本地业务事务并写事务记录
    ↓
向 Broker 提交或回滚消息
    ↓
状态不确定时由 Broker 回查
    ↓
消费者按 messageId 幂等
    ↓
失败重试
    ↓
超过上限进入 DLQ / DEAD
必须重点理解：
RocketMQ 的“可靠投递”不等于业务绝对不丢。
消费者必须支持重复消息。
为什么消费记录需要唯一索引。
为什么失败记录不能直接被当成重复消息跳过。
为什么事务回查异常时要返回 UNKNOWN，而不是直接提交消息。
为什么事务消息仍然不能替代消费幂等与 DLQ。
当前审计消费者没有不可逆业务副作用，这一点不能夸大。
建议学习时间：5～8 天。
第七阶段：AI应用开发基础
难度：★★★★
重要程度：AI 应用岗位核心
完成传统后端、数据库、Redis 和异步任务之后，再进入 AI 模块。否则容易只会调用大模型 API，不会处理超时、并发、幂等和失败补偿。
学习顺序：
大模型 API
    ↓
Prompt
    ↓
结构化输出
    ↓
Function Calling
    ↓
RAG
    ↓
Memory
    ↓
Agent 编排
重点掌握：
Token 和上下文窗口。
Temperature。
System/User/Assistant 消息。
Prompt Injection。
结构化 JSON 输出。
Tool Schema。
Function Calling 的调用循环。
Embedding。
向量相似度。
Chunking。
Rerank。
混合检索。
Agent 和工作流的区别。
模型不可用时的降级策略。
LLM 调用成本和延迟。
结合 SafeGuard 学习：
Spring AI ChatClient。
FunctionToolCallback。
ToolRegistry 如何适配成 Tool Callback。
为什么不再手写 Action: 文本解析。
为什么 Spring AI 只负责模型接入和工具调用，不应吞掉业务编排层。
为什么音频和视频模型应作为专用工具。
为什么模型不可用时应该明确失败或降级，而不是静默使用其他模型冒充。
这一阶段要能够回答：
Spring AI 和 LangChain4j 如何选择？

建议回答：
当前项目已经是 Spring Boot 技术栈。
Spring AI 与配置、Bean 生命周期、可观测性集成更自然。
项目已有自己的 Agent 和 RAG 抽象。
再引入 LangChain4j 会造成两套 Tool、Memory、Agent 抽象并存。
Spring AI 并不老套，关键是是否正确使用原生 Function Calling，而不是框架名字是否“新”。
建议学习时间：7～10 天。
第八阶段：RAG与Function Calling深入
难度：★★★★☆
重要程度：AI 应用岗位核心亮点
Function Calling 重点：
工具描述如何影响模型选择。
参数 Schema 如何生成。
参数如何校验。
工具是否允许访问敏感资源。
工具超时如何处理。
工具结果如何返回模型。
如何限制最大工具调用轮次。
如何记录调用审计。
如何防止模型调用高风险工具。
RAG 重点：
文档加载
    ↓
清洗
    ↓
切块
    ↓
Embedding
    ↓
Qdrant 召回
    ↓
规则/关键词补充
    ↓
Rerank
    ↓
上下文拼接
    ↓
LLM 生成
需要重点区分：
RAG 不是数据库查询。
向量检索不保证事实正确。
Chunk 越大并不一定越好。
TopK 越大可能引入更多噪声。
Rerank 的作用是重新排序，不是重新召回。
Function Calling 负责“做事”，RAG 负责“找知识”。
Agent 不应该成为不可控的无限循环。
建议学习时间：7～12 天。
第九阶段：音视频AI模型服务
难度：★★★★★
重要程度：AI 应用开发加分项，纯后端岗位了解架构即可
后端岗位不要求深入推导模型公式，但必须理解模型服务工程化。
音频方向：
Wav2Vec2。
ASVspoof。
Bonafide 与 Spoof。
EER。
音频采样率和预处理。
AASIST 的基本定位。
视频方向：
XceptionNet。
FaceForensics++。
DFDC。
视频抽帧。
人脸裁剪。
帧级预测与视频级聚合。
压缩、低清晰度对检测的影响。
工程侧重点：
Python Flask/FastAPI 推理服务。
模型预热。
CPU/GPU 设备选择。
显存并发限制。
模型超时。
批处理。
模型版本固定。
模型切换。
灰度与回滚。
模型评估指标。
这里必须保持真实性：
当前真正就绪的是一个音频模型和一个视频模型。
其他模型是候选适配器和目录。
没有权重和完整推理实现时，不能声称模型已经可用。
正式数据集受授权和凭证限制，不能把目录创建等同于下载完成。
建议学习时间：后端岗位3～5天，AI应用岗位10天以上。
第十阶段：高可用、可观测性与性能优化
难度：★★★★★
重要程度：架构核心，也是最后学习的部分
这一阶段需要把前面的知识串起来。
重点学习：
Redis Sentinel。
MySQL 主从复制。
GTID。
复制延迟。
读写分离。
写后读一致性。
冷热数据分离。
MinIO 对象存储。
熔断、降级和恢复。
Bulkhead。
链路追踪。
Prometheus 指标。
Zipkin。
P50、P95、P99。
吞吐量和错误率。
故障注入。
容量规划。
结合项目重点理解：
Redis 故障切换后，MySQL 幂等为什么仍然成立。
MySQL 从库为什么不能查询刚创建的任务。
为什么不进行全局自动读写分离。
为什么归档要使用主键游标。
为什么媒体文件适合 MinIO，任务状态适合 MySQL/Redis。
为什么熔断不是简单地 catch Exception。
为什么平均响应时间不能代表尾延迟。
为什么没有真实压测数据就不能写“支持十万 QPS”。
建议学习时间：10～15 天。
四个模块的总体顺序
如果严格按照你划分的四大模块，可以按下面顺序推进：
顺序	模块	原因
1	前端/微信小程序概览	最简单，从系统入口理解完整请求链路
2	传统后端技术栈	三类岗位共同基础，是后续模块的承载层
3	数据库与中间件	进入事务、并发、缓存、MQ和高可用
4	AI模块	建立在后端、数据库和异步治理能力之上
5	综合架构与性能	把四个模块串成完整项目，难度最高

但实际完善代码时，建议把“传统后端”和“数据库与中间件”交叉进行：
普通接口
→ MySQL
→ 异步任务
→ Redis 幂等
→ Java 并发
→ RocketMQ
→ AI Function Calling
→ RAG
→ 音视频模型
→ HA 和压测
针对不同岗位的学习重心
后端开发
优先级：
Java/Spring
> MySQL
> 并发与线程池
> Redis
> RocketMQ
> 幂等与一致性
> 性能和可观测性
> AI 调用原理
> 前端
面试重点是：为什么这样设计、异常时是否仍然正确。
全栈开发
优先级：
Spring Boot
> REST/JWT/文件上传/SSE
> MySQL
> Vue/微信小程序请求链路
> Redis
> AI 功能接入
> MQ与高可用
需要能独立演示从页面到数据库、AI 服务的完整闭环。
AI应用开发
优先级：
Spring Boot/Python API
> Function Calling
> RAG
> Prompt与结构化输出
> 模型服务工程化
> Redis与异步任务
> 熔断、限流和可观测性
> 模型训练原理
AI 应用岗位不是纯算法岗。相比从头训练大模型，更看重：
如何把模型可靠地接入业务。
如何设计工具调用。
如何减少幻觉。
如何治理延迟、并发和成本。
模型失败时系统是否仍然可用。
最推荐的实际完善顺序
接下来逐模块完善项目时，建议严格按这个次序：
传统后端基础与安全规范。
MySQL 表设计、事务、索引与任务状态机。
Redis 缓存、幂等、限流和分布式锁。
Java 并发、线程池、异步任务和背压。
RocketMQ 事务消息、消费幂等和 DLQ。
Spring AI 与原生 Function Calling。
RAG、Qdrant、Memory 和 Agent 编排。
音视频模型服务与模型切换。
MinIO、冷热分离、主从复制和 Sentinel。
监控、链路追踪、压测、故障演练和项目文档。
最后再梳理 Vue 和微信小程序的全链路演示。
第一轮建议从“传统后端基础与安全规范”开始。它相对简单，也能为后面的数据库、中间件和 AI 模块建立统一的接口、异常、鉴权、日志和测试标准。
